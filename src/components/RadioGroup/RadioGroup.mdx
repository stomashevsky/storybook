import { RadioGroup } from "./";
import { ArgTypes, Canvas, Controls, Meta, Subtitle, Title } from '@storybook/blocks';
import { Hero } from '@storybookComponents/Hero';
 
import * as RadioGroupStories from './RadioGroup.stories';
 
<Meta of={RadioGroupStories} />
 
<Title>RadioGroup</Title>
<Subtitle>Semantic radio option selection</Subtitle>

<Hero>
  <RadioGroup defaultValue="chocolate" onChange={() => {}} aria-label="Options">
      <RadioGroup.Item value="chocolate">Chocolate</RadioGroup.Item>
      <RadioGroup.Item value="vanilla">Vanilla</RadioGroup.Item>
      <RadioGroup.Item value="yes">Yes</RadioGroup.Item>
  </RadioGroup>
</Hero>

## Usage
```jsx
import { RadioGroup } from "@/components/ui/RadioGroup";
```
<Canvas of={RadioGroupStories.Base} />

## Reference

<h3 id="ref-radiogroup" data-heading-label="Root">`RadioGroup`</h3>
<ArgTypes of={RadioGroupStories.Base} />

<h3 id="ref-radiogroup-item" data-heading-label="Item">`RadioGroup.Item`</h3>

<ArgTypes of={RadioGroup.Item} />


## Examples
### Direction
Use `direction` to control whether the items flow in a column or row. An optimal `gap` will be dynamically applied based on the orientation.

<Canvas of={RadioGroupStories.Direction} />
<div className="canvas-controls-only">
<Controls of={RadioGroupStories.Direction}/>
</div>

### Group disabled
Use `disabled` on `RadioGroup` to disable all radio options.

<Canvas of={RadioGroupStories.GroupDisabled} />
<div className="canvas-controls-only">
<Controls of={RadioGroupStories.GroupDisabled}/>
</div>

### Item disabled
Use `disabled` on single `RadioGroup.Item` to disable only that option.
<Canvas of={RadioGroupStories.ItemDisabled} />
<div className="canvas-controls-only">
<Controls of={RadioGroupStories.ItemDisabled}/>
</div>


### Complex layout
Instrument complex layouts by leveraging component composition and `className` overrides.
<Canvas of={RadioGroupStories.CustomLayout} />
